/* Basic reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Styling for body and background */
body {
    font-family: Arial, sans-serif;
    background-color: #1e1e1e;
    color: white;
    margin: 0;
    padding: 20px;
}

/* Section styles */
.section {
    padding: 20px;
    margin-bottom: 40px;
}

/* Header styling */
#header h1 {
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 10px;
    opacity: 0;
    animation: fadeIn 2s forwards;
}

#header p {
    text-align: center;
    opacity: 0;
    animation: fadeIn 3s forwards;
}

#startButton {
    display: block;
    margin: 0 auto;
    padding: 10px 20px;
    background-color: #00aaff;
    border: none;
    color: white;
    font-size: 1.2rem;
    cursor: pointer;
    opacity: 0;
    animation: fadeIn 4s forwards;
}

#results h2, #simulationAnimation h2 {
    text-align: center;
}

#stats {
    margin-top: 20px;
}

#stats div {
    margin-bottom: 10px;
}

/* Chart styling */
#chart {
    margin-top: 20px;
    display: block;
    width: 100%;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideIn {
    from { transform: translateX(-100%); }
    to { transform: translateX(0); }
}

#simulatedTrades {
    height: 150px;
    background-color: #333;
    margin-top: 20px;
    position: relative;
}
